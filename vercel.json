{
    "version": 2,
    "builds": [
      {
        "src": "target/*.jar",
        "use": "@vercel/java"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "target/*.jar"
      }
    ],
    "buildCommand": "./mvnw package",
    "outputDirectory": "target"
  }